[project]
name = "diffmpc-cbf"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "casadi>=3.8.0",
    "numpy>=2.5.3",
]

[project.scripts]
diffmpc-cbf = "diffmpc_cbf:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
